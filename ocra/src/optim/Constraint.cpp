#include "ocra/optim/Constraint.h"
#include <stdexcept>

namespace ocra
{/*
  int Constraint<Function>::UpdatableSlackVariable::cpt = 0;

  void Constraint<Function>::UpdatableSlackVariable::updateSize()
  {
    throw std::runtime_error("[Constraint<Function>::UpdatableSlackVariable::updateSize] Change of dimension for a function linked to a slack variable is not implemented");
  }

  void Constraint<Function>::makeSlacked()
  {
    if (!isSlacked())
      _slack = new UpdatableSlackVariable(_function);
  }

  void Constraint<Function>::unmakeSlacked()
  {
    if (isSlacked())
      delete _slack;
    _slack = NULL;
  }*/
}

// cmake:sourcegroup=Constraint
